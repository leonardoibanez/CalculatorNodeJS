language: node_js
node_js: 
 - "10"
 jdk:
  - oraclejdk8
addons:
  sonarcloud: true
script:
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN -Dsonar.organization=leonardoibanez-github -Dsonar.projectKey=CalculatorNodeJS -Dsonar.sources='./app' -X